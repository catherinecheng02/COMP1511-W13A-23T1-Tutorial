// Arrays demo 
// Written by Catherine Cheng (z5310517)

#include <stdio.h>
#define SIZE 3
int main(void) {
    // What is an array?
    // data type
    // collection of variables

    // How do we initialise an array?
    int my_array[SIZE] = {7, 3, 1};

    // How do we print out an array?
    int i = 0;
    while (i < SIZE) {
        printf("%d\n", array[i]);
        i++;
    }
    
    
    return 0;
}
